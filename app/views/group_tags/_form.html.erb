<%= form_for(@group_tag) do |f| %>
  <% if @group_tag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@group_tag.errors.count, "error") %> prohibited this group_tag from being saved:</h2>

      <ul>
      <% @group_tag.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
