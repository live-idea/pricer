<%= simple_form_for(@category) do |f| %>
  <%= f.error_notification %>

  <div class="inputs">

    <%= f.input :name %>

    <%= f.input :group_id %>

  </div>

  <div class="actions">
    <%= f.button :submit %>
  </div>
<% end %>
